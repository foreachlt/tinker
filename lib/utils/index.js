/**
 * Utils module.
 */
module.exports = {
  /**
   * Diff module.
   * @type {object}
   */
  diff: require('./diff'),

  /**
   * Github module.
   * @type {object}
   */
  github: require('./github'),

  /**
   * Logger modyle.
   * @type {object}
   */
  logger: require('./logger')
};
